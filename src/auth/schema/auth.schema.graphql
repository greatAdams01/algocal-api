type AuthData {
  creatorId: ID!
  token: String!
}


type Query {
  login(email: String!, password: String!): AuthData!
}

input CreatorInput {
  name: String!
  password: String!
  email: String!
}



type Mutation {
  register(Inputs: CreatorInput!): Creator!
}